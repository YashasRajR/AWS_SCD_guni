// Reserved for admin user-management input validation in a later phase
// (e.g. suspend/deactivate). Nothing to validate yet — the /me endpoints
// this phase exposes live in the attendees module.
export {};
